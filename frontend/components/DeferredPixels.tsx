"use client";

// DeferredPixels is intentionally left empty — base pixel scripts are injected
// directly in the root layout so the base code is present in the page head.
export default function DeferredPixels() {
  return null;
}
